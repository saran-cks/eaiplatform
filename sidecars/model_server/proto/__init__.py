"""Generated gRPC stubs live here. The generated `embedding_pb2_grpc` does a
flat `import embedding_pb2`, so put this directory on sys.path to resolve it
without editing generated code."""
import os
import sys

sys.path.insert(0, os.path.dirname(__file__))
