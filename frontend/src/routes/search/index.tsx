/** Retrieval explorer over GET /search. Built in F4. Scaffold placeholder. */
export function SearchPage() {
  return (
    <div className="flex h-full flex-col items-center justify-center gap-2 p-6">
      <h2 className="font-accent text-base">search</h2>
      <p className="text-sm text-muted-foreground">retrieval explorer — wired in F4</p>
    </div>
  );
}
