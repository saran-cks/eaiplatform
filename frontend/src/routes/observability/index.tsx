/** Traces / evals / datasets / drift / feedback. Built in F4. Scaffold placeholder. */
export function ObservabilityPage() {
  return (
    <div className="flex h-full flex-col items-center justify-center gap-2 p-6">
      <h2 className="font-accent text-base">observability</h2>
      <p className="text-sm text-muted-foreground">
        traces · evals · datasets · drift — wired in F4
      </p>
    </div>
  );
}
